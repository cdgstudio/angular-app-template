import { WidgetLoaderDirective } from './widget-loader.directive';

describe('WidgetLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
