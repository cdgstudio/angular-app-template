import { CreateListPipe } from './create-list.pipe';

describe('CreateListPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateListPipe();
    expect(pipe).toBeTruthy();
  });
});
